// Global configuration for all API routes
// This ensures API routes are always dynamic and never prerendered
export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';
export const revalidate = 0;
